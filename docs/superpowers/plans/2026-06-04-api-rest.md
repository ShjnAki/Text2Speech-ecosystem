# TTS API REST (FastAPI) — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construire une API REST FastAPI sur le VPS qui reçoit du texte, appelle ElevenLabs, et retourne un `.mp3` — sécurisée par JWT, sans base de données.

**Architecture:** FastAPI expose `/auth/login` (retourne un JWT) et `/tts` (protégé par Bearer token). Les utilisateurs sont stockés dans `.env` en JSON avec mots de passe bcryptés. L'appel ElevenLabs se fait via le SDK officiel Python, le fichier temporaire est supprimé après streaming.

**Tech Stack:** Python 3.11+, FastAPI, uvicorn, python-jose[cryptography], passlib[bcrypt], elevenlabs SDK, python-dotenv, pytest, httpx (tests)

---

## Structure des fichiers

```
api/
├── main.py              # FastAPI app : montage des routes
├── auth.py              # Chargement users, vérif password, création/décodage JWT
├── tts.py               # Appel ElevenLabs, strip markdown, streaming
├── utils.py             # strip_markdown() uniquement
├── create_user.py       # Script CLI : génère la ligne .env pour un utilisateur
├── .env.example         # Template de configuration
├── requirements.txt     # Dépendances avec versions fixées
├── tts-api.service      # Service systemd prêt à déployer
└── nginx.conf.example   # Config Nginx reverse proxy (commentée, pour HTTPS futur)

tests/
├── test_utils.py        # Tests strip_markdown
├── test_auth.py         # Tests JWT, vérification mot de passe
└── test_api.py          # Tests endpoints HTTP (login, tts)
```

---

## Tâche 1 : Scaffolding et dépendances

**Fichiers :**
- Créer : `api/requirements.txt`
- Créer : `api/.env.example`
- Créer : `tests/__init__.py`

- [ ] **Étape 1 : Créer `api/requirements.txt`**

```
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
elevenlabs==1.2.2
python-dotenv==1.0.1
python-multipart==0.0.9
httpx==0.27.0
pytest==8.2.0
pytest-asyncio==0.23.6
```

- [ ] **Étape 2 : Créer `api/.env.example`**

```ini
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
ELEVENLABS_VOICE_ID=your_voice_id_here
JWT_SECRET=changez_moi_avec_une_longue_chaine_aleatoire_32chars_minimum
JWT_EXPIRE_DAYS=7
TTS_API_PORT=8000
# Générer les entrées avec : python create_user.py email@example.com motdepasse
USERS='[{"email":"admin@example.com","password_hash":"$2b$12$..."}]'
```

- [ ] **Étape 3 : Créer le répertoire de tests**

```bash
mkdir -p tests && touch tests/__init__.py
```

- [ ] **Étape 4 : Installer les dépendances dans un venv**

```bash
cd api && python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
```

Attendu : installation sans erreur, `pip list` montre fastapi, uvicorn, etc.

- [ ] **Étape 5 : Commit initial**

```bash
git add api/requirements.txt api/.env.example tests/__init__.py .gitignore
git commit -m "chore: scaffolding projet API TTS (deps + env example)"
```

---

## Tâche 2 : Utilitaire strip_markdown

**Fichiers :**
- Créer : `api/utils.py`
- Créer : `tests/test_utils.py`

- [ ] **Étape 1 : Écrire les tests (TDD — ils doivent échouer)**

Créer `tests/test_utils.py` :

```python
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'api'))

from utils import strip_markdown


def test_supprime_titres():
    assert strip_markdown("# Titre\n## Sous-titre") == "Titre\nSous-titre"


def test_supprime_gras_et_italique():
    assert strip_markdown("**gras** et _italique_") == "gras et italique"


def test_supprime_liens():
    assert strip_markdown("[texte](https://example.com)") == "texte"


def test_supprime_code_inline():
    assert strip_markdown("`code`") == "code"


def test_supprime_blocs_code():
    assert strip_markdown("```python\nprint('hi')\n```") == "print('hi')"


def test_supprime_listes():
    assert strip_markdown("- item1\n* item2") == "item1\nitem2"


def test_texte_sans_markdown_inchange():
    assert strip_markdown("Bonjour le monde") == "Bonjour le monde"


def test_chaine_vide():
    assert strip_markdown("") == ""


def test_supprime_gras_double_underscore():
    assert strip_markdown("__gras__") == "gras"
```

- [ ] **Étape 2 : Lancer les tests — vérifier qu'ils échouent**

```bash
cd api && source .venv/bin/activate && pytest ../tests/test_utils.py -v
```

Attendu : `ImportError: No module named 'utils'` ou `ModuleNotFoundError`.

- [ ] **Étape 3 : Implémenter `api/utils.py`**

```python
import re


def strip_markdown(text: str) -> str:
    """Supprime la syntaxe Markdown avant envoi à ElevenLabs."""
    # Blocs de code (``` ... ```)
    text = re.sub(r'```[\s\S]*?```', lambda m: m.group(0).split('\n', 1)[-1].rsplit('\n', 1)[0], text)
    # Titres (#, ##, etc.)
    text = re.sub(r'^#{1,6}\s+', '', text, flags=re.MULTILINE)
    # Liens [texte](url) → texte
    text = re.sub(r'\[([^\]]+)\]\([^)]+\)', r'\1', text)
    # Gras/italique (**texte**, __texte__, *texte*, _texte_)
    text = re.sub(r'\*\*(.+?)\*\*', r'\1', text)
    text = re.sub(r'__(.+?)__', r'\1', text)
    text = re.sub(r'\*(.+?)\*', r'\1', text)
    text = re.sub(r'_(.+?)_', r'\1', text)
    # Code inline (`code`)
    text = re.sub(r'`([^`]+)`', r'\1', text)
    # Listes (- item, * item)
    text = re.sub(r'^[\-\*]\s+', '', text, flags=re.MULTILINE)
    # Nettoyage espaces multiples
    text = re.sub(r'\n{3,}', '\n\n', text)
    return text.strip()
```

- [ ] **Étape 4 : Relancer les tests — vérifier qu'ils passent**

```bash
pytest ../tests/test_utils.py -v
```

Attendu : tous les tests `PASSED`.

- [ ] **Étape 5 : Commit**

```bash
git add api/utils.py tests/test_utils.py
git commit -m "feat(api): utilitaire strip_markdown avec tests"
```

---

## Tâche 3 : Authentification JWT

**Fichiers :**
- Créer : `api/auth.py`
- Créer : `tests/test_auth.py`

- [ ] **Étape 1 : Écrire les tests**

Créer `tests/test_auth.py` :

```python
import sys, os, json
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'api'))

import pytest
from unittest.mock import patch
from auth import verify_user, create_access_token, decode_access_token, AuthError

# Hash bcrypt de "motdepasse" généré avec passlib
HASH_MOTDEPASSE = "$2b$12$EixZaYVK1fsbw1ZfbX3OXePaWxn96p36WQoeG6Lruj3vjPGga31lW"

FAKE_USERS = json.dumps([
    {"email": "test@example.com", "password_hash": HASH_MOTDEPASSE}
])


def test_verify_user_credentials_valides():
    with patch.dict(os.environ, {"USERS": FAKE_USERS}):
        assert verify_user("test@example.com", "motdepasse") is True


def test_verify_user_mauvais_mot_de_passe():
    with patch.dict(os.environ, {"USERS": FAKE_USERS}):
        assert verify_user("test@example.com", "mauvais") is False


def test_verify_user_email_inconnu():
    with patch.dict(os.environ, {"USERS": FAKE_USERS}):
        assert verify_user("inconnu@example.com", "motdepasse") is False


def test_create_et_decode_token():
    with patch.dict(os.environ, {"JWT_SECRET": "secret_test_32chars_padding_here", "JWT_EXPIRE_DAYS": "7"}):
        token = create_access_token("test@example.com")
        payload = decode_access_token(token)
        assert payload["sub"] == "test@example.com"


def test_decode_token_invalide_leve_erreur():
    with patch.dict(os.environ, {"JWT_SECRET": "secret_test_32chars_padding_here"}):
        with pytest.raises(AuthError):
            decode_access_token("token.invalide.ici")
```

- [ ] **Étape 2 : Vérifier que les tests échouent**

```bash
pytest ../tests/test_auth.py -v
```

Attendu : `ImportError: No module named 'auth'`.

- [ ] **Étape 3 : Implémenter `api/auth.py`**

```python
import json
import os
from datetime import datetime, timedelta, timezone

from jose import JWTError, jwt
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")


class AuthError(Exception):
    pass


def _load_users() -> list[dict]:
    """Charge la liste des utilisateurs depuis la variable d'environnement USERS."""
    raw = os.getenv("USERS", "[]")
    try:
        return json.loads(raw)
    except json.JSONDecodeError:
        raise AuthError("Variable d'environnement USERS invalide (JSON malformé)")


def verify_user(email: str, password: str) -> bool:
    """Vérifie email + mot de passe. Retourne True si valides."""
    users = _load_users()
    for user in users:
        if user["email"] == email:
            return pwd_context.verify(password, user["password_hash"])
    return False


def create_access_token(email: str) -> str:
    """Crée un JWT signé pour l'utilisateur donné."""
    secret = os.getenv("JWT_SECRET", "")
    expire_days = int(os.getenv("JWT_EXPIRE_DAYS", "7"))
    expire = datetime.now(timezone.utc) + timedelta(days=expire_days)
    payload = {"sub": email, "exp": expire}
    return jwt.encode(payload, secret, algorithm="HS256")


def decode_access_token(token: str) -> dict:
    """Décode et valide un JWT. Lève AuthError si invalide ou expiré."""
    secret = os.getenv("JWT_SECRET", "")
    try:
        return jwt.decode(token, secret, algorithms=["HS256"])
    except JWTError as e:
        raise AuthError(f"Token invalide ou expiré : {e}")
```

- [ ] **Étape 4 : Relancer les tests**

```bash
pytest ../tests/test_auth.py -v
```

Attendu : tous `PASSED`.

- [ ] **Étape 5 : Commit**

```bash
git add api/auth.py tests/test_auth.py
git commit -m "feat(api): authentification JWT + vérification bcrypt avec tests"
```

---

## Tâche 4 : Script utilitaire create_user.py

**Fichiers :**
- Créer : `api/create_user.py`

- [ ] **Étape 1 : Implémenter `api/create_user.py`**

```python
#!/usr/bin/env python3
"""
Script utilitaire pour générer un hash bcrypt à ajouter dans .env

Usage :
    python create_user.py email@example.com monmotdepasse
"""
import sys
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")


def main():
    if len(sys.argv) != 3:
        print("Usage : python create_user.py <email> <motdepasse>", file=sys.stderr)
        sys.exit(1)

    email = sys.argv[1]
    password = sys.argv[2]
    password_hash = pwd_context.hash(password)

    print(f'\nAjouter dans .env (dans le tableau JSON de USERS) :')
    print(f'{{"email":"{email}","password_hash":"{password_hash}"}}')
    print()
    print("Exemple de variable USERS complète :")
    print(f'USERS=\'[{{"email":"{email}","password_hash":"{password_hash}"}}]\'')


if __name__ == "__main__":
    main()
```

- [ ] **Étape 2 : Tester manuellement**

```bash
cd api && source .venv/bin/activate && python create_user.py admin@example.com motdepasse123
```

Attendu : affiche un hash bcrypt (`$2b$12$...`) et la ligne à mettre dans `.env`.

- [ ] **Étape 3 : Commit**

```bash
git add api/create_user.py
git commit -m "feat(api): script create_user.py pour générer les hash bcrypt"
```

---

## Tâche 5 : Logique TTS (ElevenLabs)

**Fichiers :**
- Créer : `api/tts.py`

Note : cette tâche n'est pas testable automatiquement sans clé ElevenLabs réelle. Le test se fera manuellement après déploiement.

- [ ] **Étape 1 : Implémenter `api/tts.py`**

```python
import os
import tempfile
from pathlib import Path

from elevenlabs.client import ElevenLabs
from elevenlabs import save

from utils import strip_markdown

MAX_CHARS = 2500


class TTSError(Exception):
    pass


def generate_tts_audio(text: str) -> bytes:
    """
    Nettoie le texte, appelle ElevenLabs, retourne les bytes mp3.
    Lève TTSError si le texte est trop long ou si l'API échoue.
    """
    text_propre = strip_markdown(text)

    if len(text_propre) > MAX_CHARS:
        raise TTSError(
            f"Le texte dépasse {MAX_CHARS} caractères après nettoyage "
            f"({len(text_propre)} caractères). Veuillez le raccourcir."
        )

    api_key = os.getenv("ELEVENLABS_API_KEY", "")
    voice_id = os.getenv("ELEVENLABS_VOICE_ID", "")

    if not api_key or not voice_id:
        raise TTSError("ELEVENLABS_API_KEY ou ELEVENLABS_VOICE_ID manquant dans .env")

    client = ElevenLabs(api_key=api_key)

    # Génère l'audio en streaming puis sauvegarde dans un fichier temporaire
    with tempfile.NamedTemporaryFile(suffix=".mp3", delete=False) as tmp:
        tmp_path = tmp.name

    try:
        audio = client.generate(
            text=text_propre,
            voice=voice_id,
            model="eleven_multilingual_v2",
        )
        save(audio, tmp_path)
        return Path(tmp_path).read_bytes()
    except Exception as e:
        raise TTSError(f"Erreur ElevenLabs : {e}")
    finally:
        # Suppression du fichier temporaire après lecture
        Path(tmp_path).unlink(missing_ok=True)
```

- [ ] **Étape 2 : Commit**

```bash
git add api/tts.py
git commit -m "feat(api): intégration ElevenLabs SDK avec gestion erreurs et nettoyage temp"
```

---

## Tâche 6 : Application FastAPI principale

**Fichiers :**
- Créer : `api/main.py`
- Créer : `tests/test_api.py`

- [ ] **Étape 1 : Écrire les tests HTTP**

Créer `tests/test_api.py` :

```python
import sys, os, json
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'api'))

import pytest
from unittest.mock import patch, MagicMock
from fastapi.testclient import TestClient

HASH_MOTDEPASSE = "$2b$12$EixZaYVK1fsbw1ZfbX3OXePaWxn96p36WQoeG6Lruj3vjPGga31lW"
FAKE_USERS = json.dumps([{"email": "test@example.com", "password_hash": HASH_MOTDEPASSE}])
FAKE_ENV = {
    "USERS": FAKE_USERS,
    "JWT_SECRET": "secret_test_32chars_padding_here_ok",
    "JWT_EXPIRE_DAYS": "7",
    "ELEVENLABS_API_KEY": "fake",
    "ELEVENLABS_VOICE_ID": "fake_voice",
    "TTS_API_PORT": "8000",
}


@pytest.fixture
def client():
    with patch.dict(os.environ, FAKE_ENV):
        from main import app
        return TestClient(app)


def test_login_credentials_valides(client):
    response = client.post("/auth/login", json={"email": "test@example.com", "password": "motdepasse"})
    assert response.status_code == 200
    data = response.json()
    assert "access_token" in data
    assert data["token_type"] == "bearer"


def test_login_credentials_invalides(client):
    response = client.post("/auth/login", json={"email": "test@example.com", "password": "mauvais"})
    assert response.status_code == 401


def test_tts_sans_token_retourne_401(client):
    response = client.post("/tts", json={"text": "Bonjour"})
    assert response.status_code == 401


def test_tts_avec_token_valide_appelle_elevenlabs(client):
    # Login pour obtenir un token
    login_resp = client.post("/auth/login", json={"email": "test@example.com", "password": "motdepasse"})
    token = login_resp.json()["access_token"]

    # Mock ElevenLabs
    with patch("tts.generate_tts_audio", return_value=b"fake_mp3_bytes"):
        response = client.post(
            "/tts",
            json={"text": "Bonjour"},
            headers={"Authorization": f"Bearer {token}"},
        )
    assert response.status_code == 200
    assert response.headers["content-type"] == "audio/mpeg"


def test_tts_texte_trop_long_retourne_400(client):
    login_resp = client.post("/auth/login", json={"email": "test@example.com", "password": "motdepasse"})
    token = login_resp.json()["access_token"]

    long_text = "A" * 2600
    response = client.post(
        "/tts",
        json={"text": long_text},
        headers={"Authorization": f"Bearer {token}"},
    )
    assert response.status_code == 400
```

- [ ] **Étape 2 : Vérifier que les tests échouent**

```bash
pytest ../tests/test_api.py -v
```

Attendu : `ImportError: No module named 'main'`.

- [ ] **Étape 3 : Implémenter `api/main.py`**

```python
"""
API REST TTS — FastAPI
======================
Déploiement sur VPS Contabo (Debian) avec systemd.

Démarrage manuel (dev) :
    cd api && source .venv/bin/activate
    uvicorn main:app --host 0.0.0.0 --port 8000 --reload

Démarrage production (systemd) :
    sudo systemctl start tts-api
"""
import os
from dotenv import load_dotenv

load_dotenv()

from fastapi import FastAPI, Depends, HTTPException, status
from fastapi.responses import Response
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from pydantic import BaseModel

from auth import verify_user, create_access_token, decode_access_token, AuthError
from tts import generate_tts_audio, TTSError

app = FastAPI(title="TTS API", version="1.0.0")
security = HTTPBearer()


# ─── Schémas ──────────────────────────────────────────────────────────────────

class LoginRequest(BaseModel):
    email: str
    password: str


class TTSRequest(BaseModel):
    text: str


# ─── Dépendance d'authentification ────────────────────────────────────────────

def get_current_user(credentials: HTTPAuthorizationCredentials = Depends(security)) -> str:
    """Extrait et valide le token JWT depuis le header Authorization."""
    try:
        payload = decode_access_token(credentials.credentials)
        return payload["sub"]
    except AuthError:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Token invalide ou expiré",
            headers={"WWW-Authenticate": "Bearer"},
        )


# ─── Endpoints ────────────────────────────────────────────────────────────────

@app.post("/auth/login")
def login(request: LoginRequest):
    """Authentifie un utilisateur et retourne un token JWT."""
    if not verify_user(request.email, request.password):
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Email ou mot de passe incorrect",
        )
    token = create_access_token(request.email)
    return {"access_token": token, "token_type": "bearer"}


@app.post("/tts")
def text_to_speech(request: TTSRequest, current_user: str = Depends(get_current_user)):
    """Convertit du texte en audio MP3 via ElevenLabs."""
    try:
        audio_bytes = generate_tts_audio(request.text)
    except TTSError as e:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(e))
    return Response(content=audio_bytes, media_type="audio/mpeg")
```

- [ ] **Étape 4 : Relancer les tests**

```bash
pytest ../tests/test_api.py -v
```

Attendu : tous `PASSED`.

- [ ] **Étape 5 : Commit**

```bash
git add api/main.py tests/test_api.py
git commit -m "feat(api): endpoints /auth/login et /tts avec tests HTTP"
```

---

## Tâche 7 : Fichier systemd et config Nginx

**Fichiers :**
- Créer : `api/tts-api.service`
- Créer : `api/nginx.conf.example`

- [ ] **Étape 1 : Créer `api/tts-api.service`**

```ini
[Unit]
Description=TTS API REST (FastAPI + ElevenLabs)
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/tts-ecosystem/api
# Charge les variables d'environnement depuis le .env
EnvironmentFile=/opt/tts-ecosystem/api/.env
ExecStart=/opt/tts-ecosystem/api/.venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=5
# Logs via journald : sudo journalctl -u tts-api -f
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

Adapter `WorkingDirectory` et `ExecStart` selon l'emplacement réel sur le VPS.

- [ ] **Étape 2 : Créer `api/nginx.conf.example`**

```nginx
# ─────────────────────────────────────────────────────────────────────────────
# Config Nginx — Reverse proxy TTS API (à activer quand le DNS est disponible)
# ─────────────────────────────────────────────────────────────────────────────
# Pré-requis :
#   1. Avoir un domaine pointant sur l'IP du VPS
#   2. sudo apt install nginx certbot python3-certbot-nginx
#   3. sudo certbot --nginx -d api.tondomaine.com
#
# Copier ce fichier dans /etc/nginx/sites-available/tts-api
# Puis : sudo ln -s /etc/nginx/sites-available/tts-api /etc/nginx/sites-enabled/
#        sudo nginx -t && sudo systemctl reload nginx
# ─────────────────────────────────────────────────────────────────────────────

server {
    listen 80;
    server_name api.tondomaine.com;

    # Certbot insèrera ici la redirection HTTP→HTTPS automatiquement
    # lors de l'exécution de : sudo certbot --nginx -d api.tondomaine.com

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeout étendu pour les requêtes TTS (génération audio peut prendre quelques secondes)
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }
}

# Bloc HTTPS (généré automatiquement par Certbot — présenté ici pour référence)
# server {
#     listen 443 ssl;
#     server_name api.tondomaine.com;
#
#     ssl_certificate /etc/letsencrypt/live/api.tondomaine.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.tondomaine.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_read_timeout 60s;
#     }
# }
```

- [ ] **Étape 3 : Instructions de déploiement sur VPS**

```bash
# Sur le VPS :
# 1. Copier le projet
scp -r tts-ecosystem/ user@IP_VPS:/opt/

# 2. Créer le venv et installer les dépendances
cd /opt/tts-ecosystem/api && python3 -m venv .venv
.venv/bin/pip install -r requirements.txt

# 3. Copier et adapter le .env
cp .env.example .env && nano .env

# 4. Créer un utilisateur pour le bot
source .venv/bin/activate && python create_user.py bot@internal.local motdepasse_bot

# 5. Déployer le service systemd
sudo cp tts-api.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable tts-api
sudo systemctl start tts-api

# 6. Ouvrir le port firewall
sudo ufw allow 8000/tcp

# 7. Vérifier les logs
sudo journalctl -u tts-api -f
```

- [ ] **Étape 4 : Commit**

```bash
git add api/tts-api.service api/nginx.conf.example
git commit -m "chore(api): service systemd et config nginx commentée pour HTTPS"
```

---

## Vérification finale (self-review)

- [x] Spec coverage : `/auth/login`, `/tts`, strip_markdown, limite 2500 chars, JWT 7 jours, USERS en .env, create_user.py, tts-api.service, nginx.conf.example, ufw
- [x] Pas de placeholders
- [x] Types cohérents : `AuthError`, `TTSError`, `verify_user`, `create_access_token`, `decode_access_token`, `generate_tts_audio` utilisés de façon cohérente dans main.py
